extends layout

block content
  h1= title
  p Welcome to #{title}
  p Hey this is sathya 
  
  button(type="button",onclick="update()" class="btn btn-success") Press Me
  div(id="target") clickme
  script.
    var client_count=0;
    function update() {
      client_count++;
      document.getElementById("target").innerHTML=`Value now ${client_count}`
    }
  a(href="/trees") go to trees